---
import Markdown from "~/components/e/Markdown.astro";

const { title, i = 1 } = Astro.props;
---

<div>
  <div class="mb-1 flex items-baseline gap-4">
    <h3 class="text-accent-dark font-serif text-6xl font-bold">o{i}</h3>
    <h4 class="font-serif text-xl font-semibold">{title}</h4>
  </div>
  <Markdown><slot /></Markdown>
</div>
