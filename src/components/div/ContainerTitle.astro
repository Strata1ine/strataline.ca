---
import clsx from "clsx";
import { Pos } from "~/components/variants.ts";

const { title, class: customClass = "", variant = Pos.Left } = Astro.props;
---

<div class={clsx(`max-w-inner mb-14 lg:mx-auto`, customClass)}>
  <h2
    class={clsx(
      `font-serif text-4xl font-bold text-balance max-w-[20ch]`,
      variant ? "ml-auto text-right" : "mr-auto",
    )}
  >
    {title}
  </h2>
</div>
