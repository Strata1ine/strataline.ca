---
import clsx from "clsx";
export enum ContainerVariant {
  Inner,
  Outer,
  Max,
  Display,
}

export const containerMap = {
  [ContainerVariant.Inner]: "max-w-inner px-inset lg:mx-auto w-full",
  [ContainerVariant.Outer]: "max-w-outer px-inset lg:mx-auto w-full",
  [ContainerVariant.Max]: "max-w-full lg:mx-auto",
  [ContainerVariant.Display]:
    "max-w-[calc(var(--spacing-outer-span)+30rem)] lg:mx-auto",
};

const {
  as = "section",
  variant = ContainerVariant.Outer,
  class: customClass = "",
  ...slot
} = Astro.props;

// for every section that is uneven swap Pos
const Tag = as;
---

<Tag
  class={clsx(
    containerMap[variant],
    customClass,
    // if its a section, then apply margin bottom by default
    as === "section" && "my-25 sm:my-44",
  )}
  {...slot}
>
  <slot />
</Tag>
