---
import clsx from "clsx";
import Container, { ContainerVariant } from "~/components/div/Container.astro";
import { Pos } from "~/components/variants.ts";

const { title, description, variant = Pos.Left } = Astro.props;
---

<Container title={title} variant={ContainerVariant.Inner}>
  <div
    class={clsx(
      "gap-inset sm:gap-8 mb-3 flex flex-col",
      variant ? "sm:flex-row-reverse" : "sm:flex-row",
    )}
  >
    <h3
      class={clsx(
        `font-serif text-3xl font-bold text-balance flex-1`,
        variant ? "text-right" : "",
      )}
    >
      {title}
    </h3>
    <p class="flex-1 font-sans text-sm">{description}</p>
  </div>

  <div class="grid grid-cols-1 gap-inset sm:gap-8 sm:grid-cols-2">
    <slot />
  </div>
</Container>
