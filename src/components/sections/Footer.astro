---
import { containerStyles } from "./styles";
import business from "@root/content/business.json";

import { Icon } from "astro-icon/components";
import Search from "@actions/Search.svelte";
---

<footer
  class="relative z-2 bg-gradient-to-b from-transparent via-white/90 py-10 to-white border-t-1 border-accent"
>
  <div class={containerStyles({ width: "inner" })}>
    <div
      class="flex flex-col items-center gap-6 sm:flex-row sm:justify-between"
    >
      <ul class="flex h-fit gap-3">
        {
          business.socials.map((social) => (
            <li class="inline-flex">
              <a
                target="_blank"
                href={social.url}
                rel="noopener noreferrer"
                aria-label={`${social.name} url`}
              >
                <Icon
                  is:inline
                  style={`color: #${social.color}`}
                  name={`${social.icon}`}
                  class="size-15 xl:size-19"
                />
              </a>
            </li>
          ))
        }
      </ul>
      <Search placeholder="Find your municipality" client:visible />
    </div>

    <div
      class="sm:gap-inset mt-15 flex flex-col items-center gap-1 text-center sm:flex-row"
    >
      <span class="desc-sm"
        ><b>© Strataline. 2025.</b> All rights reserved</span
      >

      <ul class="flex gap-6 sm:ml-auto sm:gap-12">
        <li>
          <a class="desc-sm text-url hover:underline" href="">Privacy policy</a>
        </li>
      </ul>
    </div>
  </div>
</footer>
