---
import clsx from "clsx";
const { class: customClass, href } = Astro.props;
---

<a
  href={href}
  class={clsx(
    `border-black rounded-xl border-2 font-serif font-bold inline-block py-2 px-5 xl:py-3 xl:px-6 text-lg ${customClass}`,
  )}
>
  <slot />
</a>
