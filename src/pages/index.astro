---
import Layout from "~/layouts/Layout.astro";
import Content from "@build/Content.astro";

const { index } = Astro.locals;

// console.dir(index.data.components, { depth: 20 });
---

<Layout>
  <Content components={index.data.components} />
</Layout>
