---
import Root from '@/layouts/Root.astro';
// import Menus from '@/components/modals/Menus.svelte';
import { getEntry, getCollection } from '@/content.config';
// import { generateServiceSchema } from '@/schemas/service';

const data = (await getEntry('index', 'index')).data;
const services = await getCollection('services');

/*
	schemas={{
		'@type': 'ItemList',
		numberOfItems: services.length,
		itemListElement: services.map((service) => generateServiceSchema(Astro, service)),
	}}
*/
---

<Root title={data.title} desc={data.desc} blocks={data.sections} />
