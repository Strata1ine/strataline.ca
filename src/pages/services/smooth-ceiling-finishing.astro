---
import ServicePage from './[...slug].astro';
import { getEntry } from '@/content.config';

export const prerender = true;

const service = await getEntry('services', 'smooth-ceiling-finishing');

if (!service) {
	throw new Error('Service not found: smooth-ceiling-finishing');
}
---

<ServicePage service={service} />
