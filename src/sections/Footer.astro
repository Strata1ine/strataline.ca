---
import { Icon } from 'astro-icon/components';
import * as Layout from '@/components/Layout';
import Inputs from '@/components/Inputs';

import { createSchema } from 'astro-frontmatter-cms';
import { z } from 'astro:schema';
export const schema = createSchema('Footer', (c: SchemaContext) => z.object({}));

import socials from '#/socials.json';
import business from '#/business.json';
import type { SchemaContext } from 'astro:content';
---

<Layout.Divider as="footer">
	<Layout.Container
		variant="inner"
		class="border-primary relative z-2 border-t bg-linear-to-b from-transparent via-white/90 to-white py-12 backdrop-blur-xs"
	>
		<div class="flex flex-col items-center gap-6 sm:flex-row sm:justify-between">
			<ul class="flex gap-3">
				{
					socials.map((social) => (
						<li class="contents">
							<a
								target="_blank"
								href={social.url}
								rel="noopener noreferrer"
								aria-label={social.name}
							>
								<Icon
									is:inline
									aria-hidden={true}
									style={`color: #${social.color}`}
									name={social.icon}
									class="size-15 md:size-19"
								/>
							</a>
						</li>
					))
				}
			</ul>

			<Inputs.Search
				variant="xl"
				items={business.areaServed}
				placeholder="Find your municipality"
				client:visible
			/>
		</div>

		<div class="mt-9 flex flex-col items-center gap-2 text-center sm:flex-row sm:justify-between">
			<span class="desc-base"><b>© Strataline. 2025.</b> All rights reserved.</span>

			<ul class="flex flex-wrap items-center justify-center gap-4 sm:gap-8">
				<li>
					<a class="desc-base text-cyan-800 hover:underline" data-astro-prefetch href="/privacy"
						>Privacy policy</a
					>
				</li>
				<li>
					<a class="desc-base text-cyan-800 hover:underline" data-astro-prefetch href="/tos"
						>Terms & Conditions</a
					>
				</li>
				<li>
					<a
						class="group inline-flex items-center gap-2 rounded-full border border-black/10 bg-black px-4 py-2 text-sm font-semibold text-white shadow-[0_10px_28px_rgba(0,0,0,0.12)] transition duration-200 hover:-translate-y-0.5 hover:bg-cyan-900 hover:shadow-[0_14px_34px_rgba(0,0,0,0.18)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-black/30 focus-visible:ring-offset-2"
						data-astro-prefetch
						href="/reviews"
					>
						<Icon
							is:inline
							aria-hidden={true}
							name="ph:star-fill"
							class="size-4 text-primary transition-transform duration-200 group-hover:rotate-12"
						/>
						<span>Leave a review</span>
					</a>
				</li>
			</ul>
		</div>
	</Layout.Container>
</Layout.Divider>
