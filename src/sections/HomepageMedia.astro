---
import { type SchemaOf, createSchema } from 'astro-frontmatter-cms';
import { type SchemaContext } from 'astro:content';
import { z } from 'astro:schema';
import FeaturedMediaProof from '@/components/prototype/FeaturedMediaProof.astro';

export const schema = createSchema('HomepageMedia', (_: SchemaContext) =>
	z.object({
		id: z.string().optional(),
	}),
);

export type Props = SchemaOf<typeof schema>;
const meta = Astro.props as Props;
---

<FeaturedMediaProof id={meta.id ?? 'media'} variant="homepage" />
