---
import { type SchemaOf, createSchema } from 'astro-frontmatter-cms';
import { type SchemaContext } from 'astro:content';
import { z } from 'astro:schema';
import { image } from '@/schemas';

export const schema = createSchema('ImageCarousel', (c: SchemaContext) =>
	z.object({
		id: z.string().optional(),
		content: z.array(image(c)),
	}),
);

export type Props = SchemaOf<typeof schema>;
const meta = Astro.props as Props;

import * as Layout from '@/components/Layout';
import Island from './ImageCarousel';
---

<Layout.Divider id={meta.id} class="contain-paint">
	<Island client:visible meta={meta} />
</Layout.Divider>
