---
import * as Components from "@/index";
const { components } = Astro.props;
---

{
  components.map((props: any) => {
    const Component = Components[props.type];
    return Component ? <Component {...props} /> : null;
  })
}
